# Patch: Beschreibung editierbar und Save-Button nur bei Änderungen

- Der Block **Beschreibung** enthält jetzt ein editierbares Textfeld.
- Das Feld `description` wird beim Speichern per PATCH an den Incident geschrieben.
- Der Button **Änderungen speichern** wird erst sichtbar, wenn sich eines der Eingabefelder geändert hat.
- Das gilt auch für Lookup-Felder Kunde/Ansprechpartner und für die Beschreibung.
